<?xml version="1.0" encoding="utf-8"?>
<!-- Acciones de una dirección guardada. Se abre desde el botón de puntos de la fila y desde su
     pulsación larga (ver AddressListActivity.showRowMenu). "Editar" está repetido con el toque en
     la fila a propósito: quien llega por el menú buscando qué se puede hacer tiene que encontrar
     las dos opciones, no solo la destructiva. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/action_edit_address"
        android:title="@string/address_list_action_edit" />
    <item
        android:id="@+id/action_delete_address"
        android:title="@string/address_list_action_delete" />
</menu>
