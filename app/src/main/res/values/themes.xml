<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Drivo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/drivo_primary</item>
        <item name="colorOnPrimary">@color/drivo_on_primary</item>
        <item name="colorPrimaryContainer">@color/drivo_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/drivo_on_primary_container</item>

        <item name="colorSecondary">@color/drivo_secondary</item>
        <item name="colorOnSecondary">@color/drivo_on_secondary</item>
        <item name="colorSecondaryContainer">@color/drivo_secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/drivo_on_secondary_container</item>

        <item name="android:colorBackground">@color/drivo_background</item>
        <item name="colorOnBackground">@color/drivo_on_background</item>
        <item name="colorSurface">@color/drivo_surface</item>
        <item name="colorOnSurface">@color/drivo_on_surface</item>
        <item name="colorSurfaceVariant">@color/drivo_surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/drivo_on_surface_variant</item>
        <item name="colorOutline">@color/drivo_outline</item>

        <item name="colorError">@color/drivo_error</item>
        <item name="colorOnError">@color/drivo_on_error</item>
    </style>

    <!-- 44dp, no 48: seis de estas más sus separadores (Widget.Drivo.OtpDigit + los
         android:layout_marginEnd de activity_login.xml) miden 318dp de punta a punta, y con los
         24dp de padding de cada lado de la pantalla de login eso ya no cabe en una pantalla de
         360dp de ancho (312dp disponibles) — se salía unos px por cada lado en un Moto G7 Play.
         A 44dp + 4dp de separador quedan 284dp, con margen incluso en pantallas más angostas. -->
    <style name="Widget.Drivo.OtpDigit" parent="android:Widget.EditText">
        <item name="android:layout_width">44dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:background">@drawable/bg_otp_digit</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="android:importantForAutofill">no</item>
    </style>

    <!-- Píldora del item seleccionado en el drawer (nav_view), estilo Google Keep: pegada al
         borde izquierdo del panel (sin esquina ahí) y redondeada solo en el extremo derecho, en
         vez de la píldora M3 por defecto que flota separada de ambos bordes.

         El lado derecho va en 50%, no en un dp fijo: es una RelativeCornerSize, o sea la mitad
         del alto real del item, así que da siempre un semicírculo exacto aunque cambie
         itemMinHeight. Con un valor fijo (28dp) la curva quedaba mal resuelta en cuanto el alto
         del item no era exactamente el doble.

         Left/Right en vez de Start/End a propósito — MaterialShapeDrawable no espeja estas
         esquinas en RTL, y la app no soporta RTL (itemShapeInsetStart=0 en activity_home.xml
         fija el lado izquierdo igual de literal). -->
    <style name="ShapeAppearanceOverlay.Drivo.NavigationView.Item" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">0dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeTopRight">50%</item>
        <item name="cornerSizeBottomRight">50%</item>
    </style>
</resources>
