# Valor por defecto (placeholder) para que el proyecto compile aunque local.properties
# no tenga MAPS_API_KEY todavia. El mapa no cargara tiles hasta que pongas una key real
# en local.properties (ese archivo NO se sube a git).
MAPS_API_KEY=DEFAULT_API_KEY

# URL base del backend real. No es secreta (es pública una vez instalada la app), pero vive
# aquí para poder apuntar a un entorno distinto (staging) sin tocar código ni local.properties.
TRANSPORT_API_BASE_URL=https://transport-api-opbddkqgba-pv.a.run.app
